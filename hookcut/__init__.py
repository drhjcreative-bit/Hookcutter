"""HookCut — an automated social-media video editor.

Point it at footage, it transcribes, finds hooks, plans cuts for
15/30/45/60s + extended edits, renders vertical clips with captions,
then self-audits the result and iterates.

Public entry point: hookcut.pipeline.run()
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
